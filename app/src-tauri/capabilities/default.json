{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "The splash window only; the app UI talks to the local engine, not to Tauri APIs.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
